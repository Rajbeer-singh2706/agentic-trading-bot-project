vector_db:
  index_name: "trading_bot"

retriever:
  top_K: 3
  score_threshold: 0.5

embedding_model:
  google:
    provder: "google"
    model_name: "models/text-embedding-004"

llm:
  google:
     provider: "google"
     model_name: "gemini-1.5-pro"
  groq:
     provder: "groq"
     model_name: "deepseek-r1-distill-llama-70b"

tools:
  tavily:
     max_results: 5

